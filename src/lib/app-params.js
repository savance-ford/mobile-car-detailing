// Base44 app params were used for auth-gated apps.
// This site runs as a public SEO-focused web app, so these values are not required.
// We keep a tiny stub for compatibility.
export const appParams = {
  appId: null,
  token: null,
  functionsVersion: null,
  appBaseUrl: null,
};
